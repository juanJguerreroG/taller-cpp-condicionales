#include <iostream>

int main() {
    int nota;
    std::cout << "Ingrese nota (0-100): ";
    cin >> nota;

    if (nota >= 90) {
        std::cout << "A - Aprobado";
    } else if {(nota >= 80)
        std::cout << "B - Aprobado";
    } else if {(nota >= 70)
        std::cout << "C - Aprobado";
    } else if {(nota >= 60)
        std::cout << "D - Aprobado";
    else{
        std::cout << "F - Reprobado";
    }
  
    return 0;
}
